# Zendiator Constitution

[English](Constitution.md)

この文書は、Zendiator のアーキテクチャ、設計方針、品質基準をまとめたものです。ライブラリの採用を検討する利用者と、実装に携わる開発者に向けた参照資料です。利用方法は [README](README.ja.md)、配置の詳細は [ソース構成](docs/source-layout.md) を参照してください。

ライブラリの責務と公開仕様、Source Generator、ソース構成、開発時の確認事項を示します。仕様や方針を変更するときは、実装・テスト・関連文書の整合性を保ちます。

## ライブラリの目的

Zendiator は、コンパイル時に型付きの配送コードを生成する .NET 10 / C# 14 向けの Mediator です。利用者が書くメッセージ、Handler、Behavior を、単純な登録方法と低コストな実行経路で結びます。

- 具体的なリクエスト型に対して生成した API を使います。要求の `Send(object)` や `IRequest<TResponse>` を受ける汎用的な実行時ディスパッチは設けません。
- 単一要求、複数 Handler への配送、通知、同期要求、ストリームを扱います。応答には利用者の `Result` 型や nullable 型をそのまま使えます。
- 配送のための実行時アセンブリ走査、リフレクション呼び出し、`dynamic` に依存しません。登録時の型・アセンブリ情報の記録と、実行時の配送処理は区別します。
- 最速を目指しますが、扱いやすさ、型安全性、正しさ、読みやすさを犠牲にした最適化は採用しません。
- V1 前のプレビューであり、API やアーキテクチャはまだ固定していません。変更は使いやすさ、正しさ、保守性を基準に判断し、利用者への影響を明記します。

並列通知、fire-and-forget、永続化・outbox、組み込みのログ・検証・Result 変換は現在の責務に含めません。必要なアプリケーション処理は Handler や Behavior に置きます。

## 構築とプロジェクトの境界

通常の入口は `services.AddZendiator()` です。独自の ServiceProvider、ProviderFactory、高速化モードの選択、追加の初期化呼び出しを要求しません。

```csharp
services.AddZendiator();
```

Provider やホストは標準の方法で構築します。ホストへのサービス登録中に別の `BuildServiceProvider()` を呼ぶ設計にはしません。namespace、対象アセンブリ、Behavior などの指定が必要な場合だけ、`AddZendiator` の構成ラムダを使います。

| プロジェクト | 責務 |
| --- | --- |
| `src/Zendiator.Abstractions` | メッセージ・Handler・継続・Behavior の契約と構成属性。net10.0、外部パッケージへの依存なし |
| `src/Zendiator` | DI の構成記録、登録の入口、配送用の依存保持と有効期間の管理。net10.0 |
| `src/Zendiator.SourceGenerator` | Roslyn による解析、診断、型付き API・配送・登録コードの生成。netstandard2.0 |

配布は `Zendiator.Abstractions` と `Zendiator` の 2 パッケージで、バージョンをそろえます。Generator は `Zendiator` の `analyzers/dotnet/cs` に同梱し、Roslyn を利用アプリの実行時依存にしません。DI 登録の interceptor に必要な設定はパッケージの `buildTransitive` で提供します。

利用側は Contracts、Application、Host の境界を保てます。Contracts は Abstractions のみを参照し、Handler と生成の構成を Application に置き、Host はその登録メソッドを呼びます。構成のために Contracts から Application への逆参照を作りません。

## 実行経路と依存の有効期間

実行経路は、Mediator ごとの遅延キャッシュを使う方式に一本化しています。依存の作成と破棄は DI が担い、Mediator は初めて解決したサービスを自身の有効期間中に保持します。

生成される Mediator は実装用の Resolver を継承し、Mediator と依存の保持状態を一つのオブジェクトにまとめます。この基底型は利用側の拡張点ではありません。生成された配送 API を使います。初回解決には専用のロックを使い、Mediator オブジェクト自体のモニターは使いません。

Behavior のない閉じた要求だけを扱い、単一の public な参照型 Handler を共有する構成では、小さな型付きキャッシュを使えます。他の種類のルートを含む構成などは汎用キャッシュを使います。どちらも遅延取得、依存の所有権、Mediator 間のサービス型番号の割り当て順を維持します。

マーカー宣言で基底型に `object` を明示した場合は、partial 宣言間の互換性を保つため、Resolver を別オブジェクトとして保持します。

- 登録は `TryAdd` を使い、既存登録を置き換えません。新規登録の既定は Scoped です。
- `IZendiator` を生成実装型へ直接登録します。具体型の Mediator は自動登録しません。差し替えは interface の登録で行い、必要なら実装型を手動構築します。
- `ZendiatorServiceResolver` は Mediator インスタンスに属します。サービス型ごとに、初めて必要になった時点で、その Mediator に結び付いた Provider から解決します。
- `Transient` の Handler・Behavior も、同一 Mediator 内では再利用します。これは送信ごとに新しい Transient を解決する仕様ではありません。
- インスタンスを静的キャッシュや別の Provider・Mediator のキャッシュと共有しません。DI 自身が Singleton や Scoped として共有する場合は、DI の登録に従います。
- 早期終了した場合、到達していない後続 Behavior と Handler を新たに解決・構築しません。全依存の事前解決でこの性質を壊しません。
- 初回解決は Mediator 単位で同期します。ウォーム状態の取得は初回構築用のロックを通しません。利用側の Handler・Behavior が並行呼び出しに安全かどうかは別の責任です。
- Handler・Behavior の破棄は DI が担います。Mediator 自身は `IDisposable` を実装しません。スコープの破棄は、すでに始まった処理をキャンセルしたり完了まで待ったりしません。
- 送信やストリーム列挙が終わるまでスコープを維持します。破棄開始後は、破棄が例外で終わった場合も含め、Mediator やスコープを再利用しません。
- Singleton の Mediator が Scoped の依存を保持しないようにします。遅延解決のため、検証時点が初回配送になる場合があることも説明します。

詳細は [構築と配送の有効期間](docs/optimized-dispatch.md) を参照してください。有効期間の変更は単なる性能最適化として扱わず、利用者から見える仕様変更として判断します。

## 配送の意味を保つ

Behavior は DI で解決する class、継続ノードは生成する `readonly struct` です。継続はジェネリックな `TNext` として渡し、デリゲート化やインターフェース変数への変換による割り当て・boxing を避けます。具体型を直接呼べない明示的実装や継承のケースでは、解析に基づいたインターフェース経由の呼び出しを生成します。

- Behavior は `Order` が小さいものほど外側に適用されます。型や順序の重複、契約の不整合は診断します。
- open generic の適用では、型引数の対応と制約を検査します。速度のために制約検査を省きません。
- リクエストや CancellationToken の差し替え、逐次的な複数回の `next` 呼び出しを保ちます。継続の並列実行や処理終了後の保持は保証しません。
- null、キャンセル、例外、Handler の順序、早期終了、依存の破棄の所有権を、最適化やコード移動で変えません。
- 通知には既知の通知型を扱う型消去経路があります。要求の具体型 API と混同して、すべての API に実行時の型判定がないと説明しません。
- `ref struct` 要求は同期経路で扱います。非同期経路や ref-like な応答・ストリーム要素の不正な組み合わせは診断します。
- ストリームは最初の `MoveNextAsync` で開始します。API と列挙側のキャンセルを扱い、必要な場合だけトークンを連結します。初回開始と継続列挙のコストを分けて考えます。

## Source Generator の責務分離

処理の入口は `ZendiatorGenerator.cs` です。構成、型の発見、経路の解析、出力モデルへの変換、コード生成、診断の報告を分けます。

| 配置 | 責務 |
| --- | --- |
| `Analysis/Configuration` | 対応する構成式、生成方式、namespace、対象アセンブリ、構成の一致の検査 |
| `Analysis/Discovery` | 対象の型と契約の発見、解析単位のインターフェース索引 |
| `Analysis/Routes` | 単一・複数・同期・通知・ストリームの経路と Pipeline の構築 |
| `Analysis/EmissionModelFactory.cs` | 解析結果を、生成に必要な値だけを持つモデルへ変換 |
| `Models` | 解析中の経路データと、生成への受け渡しに使うモデル |
| `Symbols` / `Diagnostics` | シンボル・ジェネリック制約の共通処理と診断定義 |
| `Emission` | 機能別のテンプレートとコード断片の生成 |

構成は、現在のコンパイルと明示したアセンブリを対象に解析します。Handler 契約から参照される正確な要求型も取り込みます。任意のユーザーコードを Generator 内で実行して設定を取得しません。

DI-first の構成は interceptor で生成済み registrar に接続します。構成ラムダの記録と生成時の構造を fingerprint で照合し、両側の正規化を一致させます。`ServiceLifetime` は実行時の登録値であり、生成構造とは分けます。未対応の構成や生成登録に接続できない呼び出しを、実行時走査へ黙ってフォールバックさせません。

DI-first と属性による構成は、同じ出力・実行の設計に従います。同一コンパイルでの DI 構成ラムダと属性構成の併用など、構成の競合は診断します。

### 増分処理の境界

Roslyn のシンボルを扱う意味解析と、値だけを扱う出力処理の境界を明確にします。

- 増分比較に渡す `GenerationModel` と `GenerationTarget` には、`Compilation`、シンボル、構文ノードを保持しません。到達可能な子モデルも同じ条件にします。
- 出力モデルは不変とし、型名、nullable 注釈、制約、順序、直接呼び出しの可否など、出力に影響する値を比較します。
- 配列は要素で比較します。`ImmutableArray` を使うだけでは構造的な等価性が得られると考えず、`EquatableArray<T>` で内容を比較します。
- インターフェースの索引と型名のキャッシュは解析単位に閉じます。シンボルを保持する解析用の契約定義も、増分出力モデルには含めません。
- mediator 本体と interceptor の位置情報を分けます。登録位置の移動だけで本体を生成し直さないようにします。
- 診断は現在の編集位置に対して報告します。生成文字列が同じであることを理由に古い診断や位置を再利用しません。
- 決定的な順序と、複数コンパイル・並行実行の独立性を保ちます。

現在は Compilation が変わると意味解析を実行し、出力モデルが変わらなければテンプレート展開を省きます。型ごとの意味解析やルートごとの出力まで増分化した設計ではありません。出力が変わる Handler の変更では本体を再生成します。この境界を説明せずに、無関係な編集の処理全体を省略できるとは主張しません。

### 読めるコード生成

静的なコードの塊は、できるだけ C# の生文字列リテラルで表現します。補間が必要なら補間付き生文字列リテラルを使い、エスケープだらけの文字列や細切れの `Append` を避けます。

動的なコード断片は、役割の分かる小さなメソッドに分けます。多くの無関係な引数を渡す代わりに、対象、経路、契約など意味のある単位にまとめます。ただし、すべてを保持する巨大な Context や、利用箇所のない汎用 Builder は作りません。

`SourceEmitter` は完成したモデルを読み、`StringBuilder` は呼び出し内で作ります。機能ごとのメソッドには出力先、対象の経路、必要なら経路番号を渡します。生成の順序を隠す副作用や、複数回の生成で共有する可変出力状態を持たせません。

## ソースの配置と変更管理

- 同じ責務のファイルを同じフォルダに置きます。ライブラリ、Generator、テスト、サンプル、ベンチマークの依存方向を崩しません。
- 原則は 1 型 1 ファイルです。密接な小さな補助型や同名インターフェースの型引数違いなどは、読みやすさを損なわない範囲でまとめられます。
- プロジェクト定義、実行入口、アセンブリ単位の構成は見つけやすい場所に残します。小さなプロジェクトを形式のためだけに深く階層化しません。
- フォルダ整理と namespace・公開 API の変更は分けます。SDK の標準ファイル取り込みを使い、不要な Compile 項目の列挙を増やしません。
- 生成コードを直して修正を済ませず、解析・テンプレート・生成スクリプトなどの生成元を修正します。
- 既存コード、標準ライブラリ、既存依存を優先します。将来使うかもしれない抽象化、設定、依存パッケージは追加しません。
- 他の開発者と変更内容を調整し、関係のない作業を壊さずに維持します。コミットは目的の分かる単位にし、機械的な移動と挙動変更を判別できるようにします。
- リリースタグ、パッケージのバージョン、リリース文書の整合性を保ちます。公開する成果物は、リリース検証に通ったものと同一にします。

README は利用者向けの入口、本書は設計判断の基準、`docs/` は詳細な仕様・構成・測定・リリース記録です。同じ説明を複製し続けず、相互にリンクします。日英の README の仕様説明を一致させます。

## 性能改善の採用条件

ゼロ割り当てを実現している経路では、その性質を維持します。その経路に割り当てを追加する最適化は採用しません。初回構築とウォーム経路、同期完了と非同期中断、配送とスコープ全体のコストを区別します。

- 実行時の性能と Generator 自体の性能を別に測ります。Generator は初回生成、無関係な編集、出力に影響する Handler 変更で時間と割り当てを確認します。
- 同じ入力・実行条件で変更前後を比較し、出力や挙動が一致することを確認します。複数回の標本とばらつきを残し、都合のよい値だけを採用しません。
- 比較結果には対象の版、SDK・ランタイム、構成、ウォームアップ、測定対象を記録します。他ライブラリとの比較でも有効期間や機能条件の違いを隠しません。
- 簡易測定は方針決定に使い、公開する性能主張は測定した版、経路、条件に限定します。非常に短い処理では測定限界を考慮し、未測定の Pipeline 段数へ結果を外挿しません。
- 利用方法、有効期間、破棄、安全性の変更を、速くなったという理由だけで見落としません。

## 品質を保つ最小限のテスト

テスト数を増やすこと自体を目的にしません。同じ保証を繰り返すテストや高価な処理は統合し、異なる不具合を検出する境界のテストは残します。

- Generator のテストは、生成 API、コンパイル診断、制約、構成の不一致、増分処理、診断位置、並行実行の独立性を確認します。
- 増分処理は最終文字列の一致だけでなく、追跡ステップでテンプレート展開が再利用されたことも確認します。不正状態への遷移と修正後の復帰も扱います。
- 各成功ケースで同じ IL 生成を繰り返しません。通常はコンパイル診断を確認し、IL 生成は代表例や参照アセンブリの作成など、必要なケースに絞ります。
- ランタイムのテストは配送の結果、Pipeline、キャンセル、順序、有効期間、破棄、ストリームの境界を確認します。割り当ての回帰テストは、明示した条件の保証として維持します。
- Generator の大きな整理では、ビルド成功だけで等価と判断せず、代表的な consumer の生成結果と診断を比較します。
- 変更箇所に対応する検証を行った後、必要な統合検証を行います。変更や失敗に応じて再検証し、実施済みの確認と残っている確認を分けて記録します。

CI の確認項目は [.github/workflows/ci.yml](.github/workflows/ci.yml) を正とします。Release ビルド、テスト、フォーマット、パッケージ内容、サンプル、パッケージ参照での smoke test、Native AOT の検証を扱います。ProjectReference での成功だけをパッケージ配布や Native AOT の保証にしません。

バージョンの定義は [Directory.Build.props](Directory.Build.props)、公開手順は [publish-nuget.yml](.github/workflows/publish-nuget.yml) に従います。AOT・トリミング警告は抑制して通すのではなく、原因を解消するか対応範囲の境界として明記します。
